import * from 'react'; 
import { Routes, Route } from "react-router-dom";

const MobileVersionApp = ({}: any) => {
  return (
      <Routes>
        <Route path="/" element={<div>Heuy</div>} />
      </Routes>
  );
};

export default MobileVersionApp
